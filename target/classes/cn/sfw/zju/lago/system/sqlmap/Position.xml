<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.sfw.zju.lago.system.dao.PositionDao">
	 <select id="getPositionNum" parameterType="map" resultType="hashmap" statementType="STATEMENT">
	 	select p.kind ,p.city,p.companySize,p.financeStage,p.industryField,SUM(p.id) as total from spider_position p 
		GROUP BY p.kind  
		<if test="city != null">
    		,${city}
  		</if>
  		<if test="companySize != null">
    		,${companySize}
  		</if>
  		<if test="financeStage != null">
    		,${financeStage}
  		</if>
  		<if test="industryField != null">
    		,${industryField}
  		</if>
		ORDER BY total DESC;
	 </select>
</mapper>